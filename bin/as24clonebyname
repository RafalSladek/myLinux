#!/bin/sh

repo=$1

src_dir="/usr/sources"
token="$as24_git_token:x-oauth-basic"
user="AutoScout24"


if [ -d "$src_dir/$repo" ]; then
	echo "[Repo already exists] $src_dir/$repo"
else
	git clone "https://$token@github.com/$user/$repo" $src_dir/$repo
fi
