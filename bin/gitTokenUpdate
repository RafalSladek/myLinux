#!/bin/sh


echo "old git url..."
git remote -v

echo "updating git oauth token..."
git remote set-url origin https://$as24_git_token:$(git remote -v | head -1 | awk -F' ' '{print $2}' | awk -F':' '{print $3}')


echo "new git url..."
git remote -v

echo "done"
